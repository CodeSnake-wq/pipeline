pipeline {
    agent any

    environment {
        EMAIL_RECIPIENT = 'aakashjain2004r@gmail.com'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
                // Add your build steps here (e.g., using Maven, Gradle, etc.)
            }
        }

        stage('Unit and Integration Tests') {
            steps {
                echo 'Running Unit and Integration Tests...'
                // Add your test steps here (e.g., using JUnit, TestNG, etc.)
            }
        }

        stage('Code Analysis') {
            steps {
                echo 'Performing Code Analysis...'
                // Add your code analysis steps here (e.g., using SonarQube, Checkstyle, etc.)
            }
        }

        stage('Security Scan') {
            steps {
                echo 'Running Security Scan...'
                // Add your security scan steps here (e.g., using OWASP ZAP, Snyk, etc.)
            }
        }

        stage('Deploy to Staging') {
            steps {
                echo 'Deploying to Staging...'
                // Add your deployment steps here (e.g., deploying to an AWS EC2 instance)
            }
        }

        stage('Integration Tests on Staging') {
            steps {
                echo 'Running Integration Tests on Staging...'
                // Add your staging integration test steps here
            }
        }

        stage('Deploy to Production') {
            steps {
                echo 'Deploying to Production...'
                // Add your production deployment steps here
            }
        }
    }

    post {
        always {
            echo 'Pipeline complete.'
        }
        success {
            mail to: 'aakashjain2004r@gmial.com',
                subject: "SUCCESS: Jenkins Pipeline - ${env.JOB_NAME} Build #${env.BUILD_NUMBER}",
                body: "The Jenkins pipeline for ${env.JOB_NAME} completed successfully.",
                attachLog: true
        }
        failure {
            mail to: 'aakashjain2004r@gmail.com',
                subject: "FAILURE: Jenkins Pipeline - ${env.JOB_NAME} Build #${env.BUILD_NUMBER}",
                body: "The Jenkins pipeline for ${env.JOB_NAME} failed. Please check the logs.",
                attachLog: true
        }
    }
}
